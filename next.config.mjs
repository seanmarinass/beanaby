/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placehold.co", "example.com", "lh3.googleusercontent.com"], // Add other domains if necessary
  },
};

export default nextConfig;
